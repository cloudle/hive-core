(ns hive.store.project
  (:require [datomic.api :as d]))

(defn add-project-using-account
  [project-name account]
  ())

(defn find-projects-for-account
  [account]
  ())

(defn find-project
  [name]
  ())